using UnityEngine;

namespace CryingSnow.StackCraft
{
    [CreateAssetMenu(menuName = "StackCraft/Special Cards/Grower Card", fileName = "Card_Grower_")]
    public class GrowerDefinition : CardDefinition
    {
        // Intentionally empty.
        // We use the Class Type (is this a GrowerDefinition?)
        // to identify the card's behavior.
    }
}
