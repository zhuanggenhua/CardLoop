---
name: stackcraft-visual-animation-parity
description: StackCraft 表面与动画一致性清单：对账卡面、材质、进度、命中、粒子、投射物和拖拽 / 移动 / 受击动画是否由当前框架等效承接。
metadata:
  type: feature
  status: 设计中
---

# StackCraft 表面 / 动画一致性清单

## 当前结论

2026-08-27 纠偏：用户已用干净 StackCraft 模板反馈当前 CardLoop 的开卡包、拖拽手感和合堆层级未对齐。所以下方 2026-08-26 及更早记录全部降级为历史线索；在完成新的源码 / Prefab / Material / 输入 / 堆叠参数对账前，不得继续引用它们证明“表面与动画已对齐”。

机制、源码映射、PlayMode 和代表性业务审计只能证明已覆盖的规则效果成立，不能证明玩家可见表面和动画已经与 StackCraft 一致。当前专项按“一比一复刻”处理：先以 StackCraft 源码 / Prefab / Material / Shader / Mesh / 资源闭包参数为主证据，截图只用于发现偏差和最终诊断。

当前状态必须表述为：

- 2026-08-26 订正：本轮已把默认玩家 UI 文案改为中文，并修正卡包图资源地址到 `PackArts_Starter` / `PackArts_Beginning`。随后重新刷新了 Unity 编译、StackCraft 参考截图、CardLoop 同态截图和 Foundation E2E 过程证据；2026-08-25 的参考图、同态图和 E2E 拼图只保留为历史线索，不再作为当前完成证据。

- 已有机制证据：Gameplay 自有框架接管了一批 StackCraft 机制效果，并完成 Starter / Beginning 代表性业务竖切审计。
- 表面 / 动画来源参数已登记并进入新框架：自有 `卡牌.fbx` + 自有 `卡牌表面.shadergraph` + `卡牌表面_*.mat` + `_BaseTex` / `_MainTex` / `_OverlayTex` 逐槽贴图映射，以及复制贴图的 Unity 导入视觉参数对账已经成为正式卡牌表面链；牌桌表现使用 StackCraft 的 XZ 桌面语义，Unity Y 只表达离桌高度；旧 SpriteRenderer 底板 / 独立插画层方案和 XY 平面适配方案均已由静态守卫禁止。
- 最新静态守卫口径：Prefab / 场景 / SO 的正向完成证据必须命中目标对象的目标组件字段；C# 默认表现参数必须命中具体字段初始化、`const float` 常量或指定赋值目标。`m_Materials`、`m_Mesh`、`m_Sprite`、`m_RenderMode`、`m_SortingOrder`、`playOnAwake`、卡牌视图 label / renderer 引用、镜头震动参数、同态场景 Harness 配置、打开卡包行动槽位 / 结果意图、战斗区域视图字段、牌桌视图设置、商贩 / 收购点偏移、测试地区生成器、默认出生点、桌面背景 / 底板生成器、投射物前摇、受击 / 命中 / 行动进度表现参数，不再允许用“文件里出现过 token / GUID”证明对齐。
- 2026-08-26 目标对照：StackCraft 参考图 `Assets/Screenshots/StackCraftReference/stackcraft-main-reference-clean.png` 与 CardLoop 同态图 `Assets/Screenshots/StackCraftParity/stackcraft-parity-current-ready.png` 均为 `1196×561`；顶部 HUD、播放图标、统计 HUD、Starter Pack / 初始卡包、右侧任务 / 配方日志、灰色桌面和底部署名均在同一目标区域命中。默认玩家文本按项目中文本地化裁决显示，不再追求英文 UI 文案一致。参考图为 2026-08-26 23:14:29 +08:00 新鲜生成，SHA256 `66635B08A3669C8F866CCF77384A5807DADB59F1DEF67BDF223042BFC9DAEA91`；当前同态图为 2026-08-26 23:15:46 +08:00 新鲜生成，SHA256 `1C6FB23A78C16DF2D1CCB70EB5499726F4C7A7C4A65CCEDE14E7659EB0BBFF07`。`FoundationStackCraftParityPlayModeTests.StackCraftParityScene_CapturesStarterPackInitialFrame` 曾因 Unity Test Runner Domain Reload 回连失败返回 `totalTests=0`，已改由项目内 Editor 自动化菜单 `Gameplay/Automation/Capture StackCraft Parity Current` 产出同态目标图证据；这不是 Gameplay 失败。连续录像 / 试玩和删除模板仍是单独门槛。
- 2026-08-25 订正：YooAsset / ResourceSystem 内容作者源收集器不再允许分组名、标签和收集器路径散落命中。`gameplay-static-preflight` 必须定位 `CollectPath: Assets` 所属分组，并在同一分组内字段级对账 `GroupName = Gameplay内容定义` 与 `AssetTags = gameplay-content`；这只证明内容作者源进入正式收集配置的静态字段闭包，不等于 YooAsset 构建、Unity 编译或运行加载验收。
- 2026-08-25 订正：牌桌放置解算、HUD 文案和 TMP 材质引用继续去弱证据。`gameplay-static-preflight` 必须逐个解析 `TabletopCardStackPlacementSolver.Solve(...)` 调用实参，确认地区规则的重叠解算迭代次数进入第三个实参；剧本日志 HUD 文案只接受父子对象和目标组件字段级证据；TMP MeshRenderer 字体材质必须从 `m_Materials` 引用列表对账。这只证明静态源码 / Prefab YAML 证据更精确，不等于最终截图或连续手感验收。
- 2026-08-25 订正：普通卡面文字不再只用 `Card_Character` / `Card_Consumable` 做代表性对账。`gameplay-static-preflight` 已把 StackCraft 12 类卡牌 Prefab 的 `Title`、`Price`、`Nutrition`、`Health` 文字节点全量纳入对象级守卫，并逐项对账到正式 `Assets/Art/Prefabs/牌桌/卡牌视图.prefab` 的 `标题`、`价格`、`营养`、`生命` 节点；这只证明卡牌普通文字字段层闭包，不等于 Unity 编译、最终截图或连续动画验收。
- 2026-08-23 订正：`TabletopCardView.StackCraftTextAnchoredPosition` 已从手写常量升级为多来源 Prefab 字段对账，必须同时命中 `PackVendor.prefab` 的 `Title / Tracker / Price`、`PackInstance.prefab` 的 `Title`、`CardBuyer.prefab` 的 `Title` 文本锚点；这些来源值不一致时不能共用一个运行时文本锚点。
- 2026-08-23 订正：`FoundationTestSceneMenu.StackCraftConvertedTextStyleHashCode` 已从手写整数升级为多来源 Prefab 字段对账，必须同时命中 `HitUI.prefab` 的 `DamageLabel`、`PackVendor.prefab` 的 `Title / Tracker / Price`、`PackInstance.prefab` 的 `Title`、`CardBuyer.prefab` 的 `Title` 的 TMP `m_TextStyleHashCode`；这些来源值不一致时不能共用一个生成器文字样式 hash。
- 2026-08-23 订正：卡牌和命中结果的内容字体必须使用项目中文 TMP 字体；StackCraft 的 `LiberationSans SDF` 只作为参考模板自身字体来源，不再作为 CardLoop 卡牌内容字体真相。StackCraft 继续提供文字位置、尺寸、字号、对齐、边距、样式 hash、字距和换行/溢出等效果参数。
- 2026-08-23 订正：HUD 的 `CardStatsUI` 三组图标 / 数字统计属于 StackCraft 玩家可见卡牌表面的一部分，已从辅助表现警告升级为默认静态预检阻塞项；缺失或不一致时 `gameplay-static-preflight` 直接失败，不再只依赖 `--strict-auxiliary-parity`。
- 2026-08-23 订正：表现参数静态守卫已去掉旧 token 辅助函数，字段默认值类证据改为解析 C# 字段初始化、`const float` 常量或 YAML 字段值；当前 `gameplay-static-preflight` 与 `--strict-auxiliary-parity` 均通过。这只提高“参数确实落到正确字段”的证明强度，不等于最终画面或连续动画已验收。
- 2026-08-23 订正：静态守卫继续新增赋值级 C# 断言，牌桌放置几何、镜头参数、商贩解锁时长、测试地区生成器、同态场景默认出生点和桌面背景 / 底板生成器必须命中对应字段、常量或赋值目标；当前 `gameplay-static-preflight --strict-auxiliary-parity` 通过。这仍只证明源码 / 作者源对账更精确，不等于最终画面或连续动画已验收。
- 2026-08-23 订正：FoundationTest 镜头场景参数和四个测试地区作者源继续从弱证据收紧为对象 / 字段块对账；镜头不再可能漏检首个 StackCraft 派生参数，地区规则边界、页眉禁放区和卡牌几何不再只靠同文件散落数字通过。当前 `gameplay-static-preflight --strict-auxiliary-parity`、代表业务审计、YAML 守卫和 `.spec` lint 均通过。这仍只证明静态证据质量提升，不等于最终画面或连续动画已验收。
- 2026-08-23 订正：静态守卫新增方法体顺序断言，牌桌坐标映射、卡牌布局、战斗区域投影、拖拽会话和拖拽输入必须在指定方法内部按 StackCraft 语义链路出现；这避免“整文件出现过 token”冒充拖拽 / 坐标 / 战斗区域表面已对齐。当前 `gameplay-static-preflight --strict-auxiliary-parity` 通过，但仍不等于最终画面或连续动画已验收。
- 2026-08-23 订正：商贩、收购点、交易和流程提示链也进入方法 / 分支级静态对账；`TabletopView`、`ScenarioRun`、`ScenarioDirector`、`TabletopCardInfoPanel`、`CurrencyCardQuery`、行动条件和 `ActionResultSettlement` 必须在对应方法内承接 StackCraft 商贩表面、解锁提示、InfoPanel 优先级、货币过滤、购买生成和出售收币语义。当前 `gameplay-static-preflight --strict-auxiliary-parity` 与代表业务审计通过，但仍不等于最终画面或连续动画已验收。
- 2026-08-23 订正：输入 / 暂停接管和商贩作者源也进入方法级静态对账；`InputSystem`、`GameStateSystem`、`CardBuyerDefinition`、`PackVendorDefinition` 必须在对应类和方法里承接 StackCraft 解锁序列锁输入 / 暂停时间、收购点货币图标、卡包生成偏移、商贩商品价格和付款状态恢复语义。当前 `gameplay-static-preflight --strict-auxiliary-parity`、代表业务审计、YAML 守卫和 `.spec` lint 均通过，但仍不等于最终画面或连续动画已验收。
- 2026-08-24 订正：剧本日志 HUD 从整段文本投影推进为 StackCraft 式 `QuestsView` / `RecipesView` 分组条目结构；正式 `ScenarioJournalPanel` 运行时生成可折叠分组头、条目按钮、hover 下划线、新条目红点和 hover 详情请求，`TabletopCardInfoPanel` 按流程提示 > 日志条目 hover > 卡牌详情的优先级显示。配方 / 行动 hover 正文已从仅显示 `ActionDefinition.Description` 订正为优先按当前行动槽位生成 StackCraft `GetFormattedIngredients` 同类材料摘要；无固定材料时才回退描述。当前静态守卫、代表性业务审计、YAML 守卫、Unity 编译和 `ScenarioJournalPanelPlayModeTests` 通过；这只证明该 HUD 交互链路已对账，不等于最终视觉或全量模板功能验收。
- 2026-08-24 订正：剧本日志 HUD 的右侧菜单折叠从 CanvasGroup 透明隐藏订正为 StackCraft `MenuToggle` 同款右滑收起；正式 `ScenarioJournalPanel` 持有 `MenuPanel` 和折叠按钮文字引用，使用 `DOAnchorPosX(..., 0.5f).SetUpdate(true)` 滑动，按钮文字在 `>>` / `<<` 间切换，日结阶段自动收起而不销毁面板。当前只是源码 / Prefab / 守卫层对账，仍不等于最终视觉或全量模板功能验收。
- 2026-08-25 订正：StackCraft `MenuToggle.HandleDayEnded` 会复用 `Toggle()`，因此日结自动收起右侧菜单时也播放点击音效；正式 `ScenarioJournalPanel` 已改为在自动收起路径发送 GameCore `AudioPlaybackRequestedEvent`，人手点击仍由既有 `UINavigationTarget` 播放，避免双播。当前 `gameplay-static-preflight --strict-auxiliary-parity`、Unity 编译和 `ScenarioJournalPanelPlayModeTests` 全类 `2/2` 通过；这只证明该 HUD 行为链路已对账，不等于最终视觉或全量模板功能验收。
- 2026-08-25 订正：StackCraft 右侧 `Quests` / `Recipes` HUD 的可见宽度依赖 `UICanvas` 的 `CanvasScaler`，不是 `ScenarioJournalPanel.MenuPanel` 的局部宽度字段；正式 `Assets/Settings/Resources/UIKitSettings.asset` 必须从 StackCraft `UIRoot.prefab` 的 `UICanvas` 派生缩放配置（`1920×1080`、按高度匹配），静态预检直接对账该项目配置，避免右侧面板在 1196×561 截图下被 3840×2160 默认设置缩窄。
- 2026-08-25 订正：StackCraft 右侧 HUD 的 `MenuToggle/Label` 文字参数必须按来源 `UIRoot.prefab` 对账到父子对象级字段：`RectTransform.m_SizeDelta = {0,0}`、`m_TextStyleHashCode = -1183493901`、字号 `32`、`m_enableKerning = 1`。正式生成器 `FoundationTestSceneMenu.CreateStackCraftJournalMenuButton` 和 `ScenarioJournalPanel.prefab` 已改为写入这些参数，静态预检按 `MenuToggle/Label` 父子关系检查，防止同名 `Label` 误判。这只证明折叠按钮字段层对账，不等于右侧 HUD 最终截图已完全一致。
- 2026-08-25 订正：StackCraft `QuestsView` / `RecipesView` 的 ScrollRect 滚动边界手感来自 `UIRoot.prefab`，其中 `m_MovementType = 1` 表示弹性滚动；正式生成器 `FoundationTestSceneMenu.CreateStackCraftJournalView` 和 `ScenarioJournalPanel.prefab` 已从 Clamped 改为 Elastic，并同步滚动条可见策略与间距。静态预检从 StackCraft 来源 Prefab 逐字段派生 `m_Horizontal`、`m_Vertical`、`m_MovementType`、`m_Elasticity`、`m_Inertia`、`m_DecelerationRate`、`m_ScrollSensitivity`、`m_HorizontalScrollbarVisibility`、`m_VerticalScrollbarVisibility`、`m_HorizontalScrollbarSpacing` 和 `m_VerticalScrollbarSpacing`，避免维护第二份手写参数清单。这只证明右侧 HUD 滚动字段层对账，不等于最终截图或连续手感已验收。
- 2026-08-25 订正：StackCraft `QuestsToggle/Label` 与 `RecipesToggle/Label` 也按 `UIRoot.prefab` 做父子对象级字段对账，包含 RectTransform 锚点、位置、尺寸、轴心，以及 TMP 文字、颜色、字号、样式 hash、对齐和字偶距。正式生成器已同步来源标签的微小 X 偏移 `0.000061035156`，防止页签文字参数继续停留在“看起来差不多”的状态。
- 2026-08-25 订正：StackCraft 右侧 HUD 的页签和折叠按钮颜色过渡目标是子文字 `Label`，不是背景 Image；正式 `ScenarioJournalPanel.prefab` 已把 `QuestsToggle`、`RecipesToggle` 和 `MenuToggle` 的 `m_TargetGraphic` 指向对应 TextMeshProUGUI，并把页签 `m_SelectedColor` 对齐为白色。静态预检新增父子对象引用守卫，防止按钮交互反馈绑回背景图。
- 2026-08-25 订正：StackCraft `UIRoot.prefab` 中 `DayTimeUI`、`CardStatsUI`、`TimeProgress`、`DayText`、`PaceImage`、三组统计图标 / 数字和 `Watermark` 都使用 Unity UI Layer（`m_Layer: 5`）。正式 `Assets/Art/Prefabs/UI/*.prefab`、`命中结果.prefab` 和 `行动进度.prefab` 已同步该 Layer；`卡牌视图`、`投射物`、`战斗区域` 仍保持世界对象 Layer 0。`FoundationTestSceneMenu` / `FoundationTitleTestSceneMenu` 在保存 UI Prefab 前递归写入 UI Layer，静态预检按对象级 `m_Layer` 和生成器保存顺序守卫，防止 HUD / 浮动 UI 回退到 Default Layer。这只证明 Layer 序列化字段闭包，不等于 Unity 编译、最终截图或完整 UI 复刻完成。
- 2026-08-25 订正：StackCraft `DayTimeUI.OnPointerClick` 的速度循环语义由 `ScenarioRun.ScenarioTimePace` 承接，而不是恢复旧 `TimeManager` 或写全局 `Time.timeScale`。实时模式下点击 HUD 主按钮会按正常 → 加速 → 暂停 → 正常循环；暂停档停止本单局实时推进，加速档按 2 倍推进本单局实时秒数；回合制和日终阶段显示暂停图标但不把它当成可手动推进状态。`ScenarioTurnPanel.prefab` 已序列化三张 StackCraft 速度图标，静态预检守卫运行时字段、点击链、倍率映射和 Prefab 图标列表。这只证明 DayTimeUI 行为源码 / Prefab 对账，不等于 PlayMode 或最终手感验收。
- 2026-08-25 订正：StackCraft `DayTimeUI.HandleDayEnded` 与 `CardStatsUI.HandleDayEnded` 的日终隐藏语义已由 `ScenarioTurnPanel` 承接：`ConfirmTurn` 和 `CardStatsUI` 的 `CanvasGroup` 在非 `Inactive` 日程阶段设为透明并关闭射线阻断，回到普通日内阶段再显示；生成器和 Prefab 均序列化到同一现有组件。`ConfirmTurn` 继续通过既有 `UINavigationTarget` 承接 StackCraft 点击音效，走 GameCore `submitSound`，不恢复旧 `DayTimeUI` / `CardStatsUI` 脚本、旧 `AudioManager` 或第二套 HUD 状态。当前只是源码 / Prefab / 静态守卫层对账，不等于 Unity 编译、PlayMode 或最终截图验收。
- 2026-08-25 订正：`gameplay-static-preflight` 不再保留“辅助表现未对齐但普通预检可通过”的软门禁。`--strict-auxiliary-parity` 仍可作为历史命令参数传入，但不会改变结果；HUD 提示职责、表面和必要反馈缺口一律作为默认预检失败处理，避免普通预检和 strict 预检形成两套验收口径。
- 2026-08-25 订正：StackCraft `QuestsView` 的 `QuestGroup.GroupName` 和 `RecipesView` 的 `RecipeDefinition.Category` 已由 Gameplay 作者源上的日志分组名承接：任务使用 `QuestDefinition.m_journalGroupName`，配方 / 行动使用 `ActionDefinition.m_journalGroupName`，运行时 `ScenarioJournalPanel` 按该字段生成多个可折叠分组；字段为空时只兜底到 `Quests` / `Recipes`，不新增 `QuestGroup` 资产、不搬 `RecipeCategory` 枚举，也不把 UI 分组伪装成规则标签。当前只是源码 / 代表性 SO / 静态守卫层对账，不等于最终视觉或全量模板功能验收。
- 2026-08-25 订正：StackCraft Starter 同态场景的右侧任务 HUD 来源必须是 Main 场景 `QuestManager` 的 `Introduction` 组第一条 `Open Starter Pack`，不是 Island 场景的 `The Basics / Open Island Pack`。正式同态任务新增为 `Assets/Gameplay/Tests/地基StackCraft同态打开初始卡包任务.asset`，标题、描述和分组按 `Assets/StackCraft/Resources/Quests/01_Introduction/introduction_01.asset` 对齐，完成条件用当前正式 `ActionCompletionQuestTaskDefinition -> test.foundation.pack.open` 承接 `PackInstance.OnClick` 玩家效果；普通 `地基测试任务.asset` 不再被同态剧本复用。
- 2026-08-23 订正：表面与动画静态守卫继续去弱证据；测试作者源生成器、交易区布局、镜头控制、投射物、牌桌放置几何、卡牌视图表面 / 卡包 / 商贩 / 受击、占位图、HUD 统计图标、行动进度 UGUI、货币卡查询、序列播放队列、卡牌尺寸和 `TabletopView` 实例化尺寸应用已经按具体方法体、类结构或字段对账。当前 `gameplay-static-preflight` 与 `--strict-auxiliary-parity` 通过，但仍不等于 Unity 编译、最终画面或连续动画已验收。
- 2026-08-26 过程证据：项目内 Editor 自动化菜单 `Gameplay/Automation/Capture Foundation E2E Current` 已复用正式 `FoundationTest.unity`、`ScenarioDirector`、牌桌交互、行动选择面板和回合推进入口生成 6 张过程图；过程拼图 `Assets/Screenshots/FoundationE2E/_contactsheet-foundation-e2e-sequence-latest.png` 写入时间为 `2026-08-26 23:09:44 +08:00`，尺寸 `3588×1122`，SHA256 `1E4DD5A9D207D1ED3E9106A635C2A4575905261F857A48F648035CB06B8F23A0`。结果文件 `Assets/Screenshots/FoundationE2E/foundation-e2e-current-result.json` 为 `pass`，记录剧本 `test.foundation.scenario` 推进到第 2 回合，行动 `test.foundation.action` 完成并生成产物 `test.foundation.product`；截图只能证明离散过程帧，不替代连续录像或试玩。
- 2026-08-23 Unity 补证：最新 CLI 静态预检、代表性业务审计、YAML 守卫和 `.spec` lint 均通过；UnitySkills editor-automation preflight 通过；`/compile/status` 显示最近一次脚本编译成功且 C# 错误数为 0；当前控制台唯一错误是 Unity Connect 登录 / 网络令牌交换失败，不属于 Gameplay 编译错误；`FoundationTest` 与 `FoundationStackCraftParityTest` 均可加载、`isDirty=false`、缺失引用数为 0，健康检查仅提示无 Light 组件；检查后已恢复 `FoundationTitleTest`。这只证明当前工程可编译、两个测试场景可回读且已恢复标题场景，不等于最终画面、连续动画或 StackCraft 全量效果验收。
- 当前边界：本轮目标对照已经完成；粒子、命中、投射物、受击、镜头、牌堆拖拽、候选 / 交易高亮、外轮廓 shader 参数、商贩付款文本、卡包实体独立表面和卡包购买烟雾项只能按源码 / Prefab / 材质 / 静态守卫 / PlayMode 截图各自覆盖范围声明。连续录像、试玩确认、全量业务资产迁移和删除 `Assets/StackCraft` 仍是单独门槛。
- 禁止口径：不得把当前目标对照自动升级成“可以删除模板”；删除参考目录必须另有用户授权、删除前依赖扫描和删除后 Unity 验证。

本表不是要求复制 StackCraft 旧 MonoBehaviour、旧单例、旧 `Resources`、旧输入或旧 UI 结构；它要求把模板承载玩家效果的表面和动画，用 CardLoop 正式对象、ResourceSystem、UIKit / 牌桌视图和 EX-GAS / GameCore 表现链等效实现，并给出来源证据。

## 审计口径

| 层级 | 说明 | 当前是否完成 |
|---|---|---|
| 机制一致 | 触发条件、规则结算、状态变化、业务参数、存档 / 任务事实。 | 当前代表范围已由静态预检、代表性业务审计和目标 PlayMode 证据覆盖；全量 StackCraft `.asset` 转换不属于当前完成口径。 |
| 表面一致 | 玩家静止画面可见的卡面、材质、图标、文字、位置、比例、颜色、进度条、详情面板。 | 来源参数已有静态守卫覆盖；StackCraft 参考图与 CardLoop 同态图均为 `1196×561`，目标对象区域已对照完成；E2E 拼图为 `3588×1122`。 |
| 动画一致 | 玩家操作和结算过程中看到的拖拽、拆堆、合堆、移动、抬升、受击、命中、投射物、粒子、镜头。 | 源码 / Prefab / 动画参数已有登记和静态守卫覆盖；E2E 截图链覆盖离散过程帧，连续手感仍需录像或试玩时再单独验收。 |
| 删除模板门槛 | 正式链路无依赖、代表性业务可追溯、表面 / 动画缺口已对账并处理、用户授权删除、删除后 Unity 验证。 | 未满足。 |

## 证据账本

| 范围 | StackCraft 证据 | 当前 CardLoop 证据 | 当前判断 |
|---|---|---|---|
| 卡牌 Prefab 分族 | `Assets/StackCraft/Prefabs/Cards/Card_*.prefab`；每类卡用 `MeshFilter` + `MeshRenderer` + 类别材质，角色卡还有 `EquipmentPanel`、`Health`；根 Collider 为 `0.8 × 0 × 1.0000002`，文本局部旋转 `x = 90`。 | 当前链路由 `CardDefinition.m_cardSurface` 声明类别材质；`TabletopView` 通过 `ResourceSystem.LoadAssetAsync<Material>` 加载；`TabletopCardView.SetSurfaceMaterial` 写入 `MeshRenderer`；正式 `Assets/Art/Prefabs/牌桌/卡牌视图.prefab` 已回读 `MeshFilter` / `MeshRenderer` / 自有 `卡牌.fbx` / 根 Collider 和文本 `x = 90`。 | **已登记并受静态守卫覆盖，结构差异已裁决**：旧 `SpriteRenderer` 表面方案、XY 牌桌姿态和旧插画层均由静态守卫禁止；Mesh / Collider / 文本姿态由 Prefab 和生成器守卫，最终静止画面仍留到整体验收截图。 |
| 角色装备面板表面 | `Card_Character.prefab` 的 `EquipmentPanel` 子物体位于 `{0,0.001,-0.525}`，使用 `EquipmentPanel.fbx`、`EquipmentPanel.mat`、Collider `0.45 × 0 × 0.15000002`；材质引用 `EquipmentPanel.shadergraph` 和 `EquipmentSlots.png`，并保留 `_CellActive0/1/2`、`_TintCellIndex = 2`、`_TintCount = 4` 等槽位着色参数。 | 当前 `卡牌视图.prefab` 已把 `装备面板` 放入现有 `角色状态`节点，随角色卡生命文本一起显示 / 隐藏；自有资源落在 `Assets/Art/Models/装备面板.fbx`、`Assets/Art/Shaders/装备面板.shadergraph`、`Assets/Art/Materials/装备面板.mat`，贴图复用自有 `Assets/Art/Sprites/StackCraft/EquipmentSlots.png`；`FoundationTestSceneMenu` 重建 Prefab 时会生成同款子物体，`TabletopCardView` 统一投影角色状态子渲染器排序。 | **已登记并受对象级静态守卫覆盖**：模型 / ShaderGraph / 贴图字节、材质参数、Prefab 子物体和生成器源码已由 `gameplay-static-preflight` 守卫；2026-08-20 Unity 编译 `0` 错误只是历史编译证据，不等于最终画面一致。最终画面仍留到整体验收截图。 |
| 卡牌材质分类 | `Assets/StackCraft/Materials/Cards/*.mat`；角色、材料、配方、资源、敌对等有不同材质，材质引用 `Card.shadergraph` 并包含 `_BaseTex`、`_MainTex`、`_OverlayTex`、`_OverlayScale`、`_OverlayOffset`、`_OverlayTint`、`_FlashAmount`；其中部分 `_MainTex` 复用其它类别贴图，配方 `_OverlayTex` 使用 `CardArts/Recipe.png` 而不是 Placeholder。 | 类别材质已迁入 `Assets/Art/Materials/卡牌表面_*.mat`，卡牌分族贴图和卡图贴图保留在 `Assets/Art/Sprites/StackCraft/Cards/*.png` 与 `Assets/Art/Sprites/StackCraft/CardArts/*.png`；`gameplay-static-preflight` 从 StackCraft 来源材质逐槽读取 GUID，再映射到项目自有复制素材 GUID，并校验复制贴图的 mipmap、边缘采样、透明和压缩等 Unity 导入视觉参数。 | **已登记并受静态守卫覆盖**：自有 `卡牌表面.shadergraph` 与 StackCraft `Card.shadergraph` 哈希一致，12 类材质的 `_BaseTex` / `_MainTex` / `_OverlayTex` 均由来源材质逐槽推导，贴图 hash 必须等同来源、GUID 必须是项目自有 GUID，导入视觉参数必须等同来源但保留项目自有 Sprite 引用能力；运行时仍通过正式 ResourceSystem 加载，不直接引用 StackCraft 旧路径。 |
| 卡面文字落点 | StackCraft 卡牌 Prefab 中 `Title` 位于 `z = 0.4`，价格 / 营养 / 生命在卡牌不同局部位置；角色生命显示当前生命数字，不显示 `当前/上限`；模板卡面不显示使用次数；模板自身 TMP 字体使用 `LiberationSans SDF`，但它不支持 CardLoop 后续中文卡牌内容。 | `卡牌视图.prefab` 现在只保留标题、价格、营养和角色生命文本；`TabletopCardView.ApplySize` 会从原始 Collider / Mesh 回读未缩放卡牌本体尺寸，并把作者源可见尺寸转换为根对象缩放，使 Mesh、文本、图标和命中范围一起对齐 StackCraft；非模板的使用次数文本已删除；文字位置、`TMP` 对齐、标题 margin、字号基准、样式 hash、字距和换行/溢出语义按 StackCraft YAML 回写，字体资产和材质引用使用项目中文 TMP 字体，Villager 生命按 StackCraft 原始 15 投影。 | **已登记并受对象级静态守卫覆盖**：生成器、Prefab 回读和静态守卫均锁定 StackCraft 文字效果参数、中文字体引用和根缩放入口；最终字号观感仍留到整体验收截图。 |
| 牌桌坐标语义 | `CardController.GetMouseWorldPosition()` 使用 `Plane(Vector3.up, Vector3.zero)`；拖拽时 `mousePos.y = DragHeight`；`CardStack.stackStep = (0, 0.002, -0.18)`；`Board` 按 X/Z 判断边界。 | `TabletopCoordinateSpace` 是当前唯一映射：二维桌面坐标 `(x, y)` 映射到 Unity 本地 `(x, height, y)`；`TabletopCardLayout`、拖拽投影、镜头、烟雾、投射物、战斗区域和 PlayMode 坐标断言均已改走该映射；战斗区域 Prefab 保持 `x = 90°` 旋转贴到 XZ 桌面。 | **源码不变量已登记并受静态守卫覆盖**：`gameplay-static-preflight` 禁止 `new Vector3(x, y, 0)` 式 XY 回流，并检查战斗区域 Prefab 旋转和本地适配颜色；最终画面仍留到整体验收截图。 |
| 桌面底板表面 | `Assets/StackCraft/Prefabs/Boards/Board01.prefab` 与 `Board02.prefab` 都使用 `Board.fbx` 的 `SkinnedMeshRenderer`；Prefab 中 `m_AABB.m_Extent = {24,0,16}` 是渲染保守包围盒，尺寸为 `48 × 0 × 32`，不能当作玩法边界；旧 `Board` 脚本在运行时用 `BakeMesh + RecalculateBounds` 从 `Board.fbx` 基础网格派生可摆放边界 `12 × 8`，并提供 `TopMargin = 1.5`。Board01 材质为 `Body_01.mat` / `Header_01.mat`，Board02 材质为 `Body_02.mat` / `Header_02.mat`。 | 当前 `FoundationTest` 与 `FoundationMapTest` 生成 Board01 纯表现节点 `牌桌底板`；`FoundationSecondMapTest` 生成 Board02 同形节点。自有资源落在 `Assets/Art/Models/牌桌.fbx`、`Assets/Art/Materials/牌桌主体_01.mat`、`Assets/Art/Materials/牌桌页眉_01.mat`、`Assets/Art/Materials/牌桌主体_02.mat`、`Assets/Art/Materials/牌桌页眉_02.mat` 和自有 `桌面简单光照.shadergraph`，不带 StackCraft 旧 `Board` 脚本；四个测试地区作者源把规则边界回写为 `x=-6, y=-4, width=12, height=8`，并用 `y=2.5, height=1.5` 的禁放区承接顶部页眉；纯表现 `SkinnedMeshRenderer.localBounds` 仍使用渲染安全尺寸 `48 × 0 × 32`。 | **已登记并受对象级静态守卫覆盖，结构差异已裁决**：模型、shader、Board01 / Board02 材质颜色、`牌桌底板` 对象 Transform、`SkinnedMeshRenderer`、Mesh、材质、渲染 AABB、运行时 BakeMesh 规则边界、页眉禁放区和生成器由 `gameplay-static-preflight` 守卫；Gameplay 只保留纯表现底板，桌面边界归正式牌桌区域规则。历史 Unity 编译曾验证无错误，最终构图仍留到整体验收截图。 |
| 场景背景表面 | `Assets/StackCraft/Scenes/Main.unity` 的 `Background` 使用 Unity 内置 Plane Mesh、`Grass.mat`，Transform 为位置 `{0,-10,0}`、旋转 `{0,180,0}`、缩放 `{10,1,10}`；`Island.unity` 使用同形背景但材质换成 `Water.mat`。 | 当前 `FoundationTest` 与 `FoundationMapTest` 生成草地背景；`FoundationSecondMapTest` 生成水面背景。自有资源落在 `Assets/Art/Textures/草地背景.png`、`Assets/Art/Materials/草地背景.mat`、`Assets/Art/Textures/水面背景.png`、`Assets/Art/Materials/水面背景.mat`；`FoundationTestSceneMenu` 重建时按 StackCraft Main / Island 参数选择材质。 | **已登记并受对象级静态守卫覆盖**：Grass / Water 贴图哈希、材质贴图导入参数、材质平铺参数、自有 GUID、`桌面背景` 对象 Transform、内置 Plane Mesh、材质引用和生成器由 `gameplay-static-preflight` 守卫；历史 Unity 编译曾验证无错误，最终材质观感仍留到整体验收截图。 |
| 卡图承载 | `CardInstance.SetDefinition` 把定义的 ArtTexture 写入材质 `_OverlayTex`，角色材质 `_OverlayScale = 0.8`、`_OverlayOffset.x = 0.1`、`_OverlayTint` 为深蓝灰。 | `TabletopCardView.SetArtwork(Sprite)` 把卡图纹理写入当前表面材质属性块 `_OverlayTex`；`卡牌视图.prefab` 已清除独立 `m_artworkRenderer`、`m_artworkPadding`、黑底透明材质和 SpriteRenderer 插画层。 | **源码参数已登记并受静态守卫覆盖**：静态守卫确认材质覆盖层路径，并禁止旧独立插画层；目标 PlayMode 卡面加载用例只证明 YooAsset 卡图进入视图，最终卡图比例观感仍留到整体验收截图。 |
| 候选高亮 | `CardInstance.SetHighlighted` / `Highlight` 在首次启用时动态创建 `Highlight` 子物体：父级为卡牌实例，使用原卡 Mesh 和 `Default_Card_Settings.outlineMaterial`，该材质引用 `CardOutline.shadergraph` 子资源；子物体本地位置为零、本地旋转为单位姿态、本地缩放为一，关闭投影和接收阴影；外轮廓厚度来自 shadergraph `_OutlineSize = 0.1`。 | 当前由 `卡牌视图.prefab` 内的 `候选高亮` 子物体承接：父级为卡牌视图，初始隐藏，本地位置 / 旋转 / 缩放分别为零 / 单位 / 一；`MeshFilter` 使用自有 `卡牌.fbx`，`MeshRenderer` 使用自有 `Assets/Art/Shaders/卡牌轮廓.shadergraph` 子资源并关闭投影 / 接收阴影；自有 `卡牌轮廓.shadergraph` 与 StackCraft `CardOutline.shadergraph` 文件哈希一致，`_OutlineSize = 0.1`；四条 Sprite 线框属于已排除的近似实现。 | **已登记并受静态守卫覆盖，结构差异已裁决**：StackCraft 运行时创建高亮对象，Gameplay 预制体预放同等子物体，原因是正式 Prefab / 作者源需要可静态回读；运行时仍只切换显隐，不保存第二份规则状态。`gameplay-static-preflight` 已按高亮对象块检查隐藏状态、Transform、Mesh、Outline 子资源和阴影参数；测试只能补充交互链路，不能替代参数闭包。 |
| 牌堆拖拽 | `CardController` 在按下时拆堆、抬高到 `DragHeight`、随鼠标移动，落下时播放拿起 / 放下音效并重新放置；`OnPointerUp` 用牌桌世界位置移动距离和 `CardSettings.clickThreshold = 0.02` 区分点击 / 拖拽；`CardStack.SetDragTargetPosition` 让首张卡立即到 `DragHeight`，尾随卡牌使用 `precedingCard.transform.position + stackStep` 与 `swaySharpness = 100` 链式跟随；拖最上层卡时移动整叠。 | `TabletopCardDragInput` 走新输入系统；`TabletopCardDragSession` 用牌桌坐标位移和 `TabletopViewSettings.m_clickThreshold = 0.02` 判断释放解释，同时低于阈值也持续刷新预览牌堆位置；`TabletopViewSettings.m_dragHeight = 0.1` 承接 StackCraft `dragHeight`；`TabletopView` 拖拽首张卡投影到 Unity Y=`0.1`，尾牌使用上一张卡当前表现位置加 StackCraft 步进；预览锚点使用实际被拖走牌段首张卡，拖最上层卡时预览整叠，拖中间卡时预览尾段；测试视图设置资产已回读 `m_stackHeightStep = 0.002`、`m_dragFollowSharpness = 100`、`m_clickThreshold = 0.02`、`m_dragHeight = 0.1`、`m_moveDurationSeconds = 0.1`。 | **源码参数已登记并受静态守卫覆盖，输入结构差异已裁决**：源码消费链和测试作者源资产由 `gameplay-static-preflight` 守卫；新输入系统替代 StackCraft 旧输入入口，`EventSystem` 只负责 UI 命中和释放目标射线，不参与卡牌阈值；低于阈值时画面仍拿起卡牌，但释放仍可按点击处理。中间牌拖拽 PlayMode 只补充链路证据，最终连续手感仍留到整体验收截图 / 录像确认。 |
| 牌堆移动补间 | StackCraft `CardSettings.moveDuration = 0.1`、`moveEase = Ease.OutQuad`；`CardInstance.SetTargetAnimated` 使用 `transform.DOMove(target, Settings.MoveDuration).SetEase(Settings.MoveEase).SetUpdate(true)`，且在 `Time.timeScale == 0` 时为移动 tween 追加 `Physics.SyncTransforms()`；`SetTargetInstant` 直接写位置并在暂停时同步物理；新建牌堆首次落位 `instant: true`。 | `TabletopViewSettings.m_moveDurationSeconds = 0.1`；`TabletopCardView.ApplyPose` 用 `DOLocalMove(pose.LocalPosition, durationSeconds)` + `Ease.OutQuad` + `SetUpdate(true)` + 暂停时 `moveTween.OnUpdate(Physics.SyncTransforms)`；`duration <= 0` 的首次落位直接写 `localPosition` 并暂停时同步物理；`TabletopView` 新实例首次落位传 0 秒，后续权威姿态变化传 0.1 秒。 | **已登记并受静态守卫覆盖**：移动时长、缓动、非缩放补间、暂停时间更新和暂停物理同步已回到 StackCraft 参数；差异是 Gameplay 使用本地坐标和 `DOLocalMove`，因为卡牌视图挂在牌桌根下并由 `TabletopCoordinateSpace` 统一映射 XZ 桌面。 |
| HUD 卡牌统计 | `CardStatsUI` 在 `UIRoot.prefab` 中把营养、货币、卡牌容量拆成三组图标 + 数字：图标使用 `Stats_Nutrition.png`、`Stats_Currency.png`、`Stats_Card.png`，`Image.preserveAspect = true`、图标高度 `30`、数字文字高度 `30`、字号 `30`，数字字体和材质使用 `LiberationSans SDF`。 | `ScenarioTurnPanel.prefab` 已用正式 UIKit 面板承接常驻 HUD，不恢复 `CardStatsUI` 管理器或 `CardManager.OnStatsChanged`；三张图标已复制到 `Assets/Art/Sprites/StackCraft/`，字节与 StackCraft 源图一致但 GUID 独立；三个数字标签改为同款 `LiberationSans SDF` 字体 / 材质，中文回合按钮和说明仍使用项目中文字体。 | **已登记并受对象级静态守卫覆盖，结构差异已裁决**：HUD 数据真相来自 `ScenarioRun.GetTabletopStats()`，UI 只读投影；`gameplay-static-preflight` 已按 `UIRoot.prefab` 的 `CardStatsUI` 源对象对账三张图标、三个数字标签、字段引用、图片 GUID、字体和材质，避免只用相似图标或项目字体冒充模板 HUD。 |
| 行动进度条 | `ProgressUI.prefab` 是 RectTransform + UGUI Image，根尺寸 `0.8 × 0.05`，灰底 `{0.25,0.25,0.25,0.8}`，黄色填充 `{1,0.7974138,0,1}`，`ProgressFill` 使用 `Square.png`、`Image.Type = Filled`、`FillMethod = Horizontal`、预览 `FillAmount = 0.725`；`ProgressUI.displayOffset = {0,0,0.55}`；`CraftingManager` 以目标牌堆世界坐标 + 偏移实例化并持续写 `Image.fillAmount = Progress / CraftingDuration`。 | `行动进度.prefab` 使用自有 `Assets/Art/Sprites/StackCraft/Square.png` 副本，文件哈希与 StackCraft 源图一致但 GUID 独立；Prefab 回写同款根尺寸、灰底、黄色填充、Filled/Horizontal/0.725 和 `m_displayOffset {0,0,0.55}`；`TabletopActionProgressView.Show` 只读行动进度，设置 `m_progressFill.fillAmount` 和同款黄色，不再做 SpriteRenderer 缩放、同锚点多条错位或暂停变色；`Tabletop.StartActionInstance` 拒绝已参与活动普通行动的卡牌再次启动普通行动。 | **已登记并受对象级静态守卫覆盖，结构差异已裁决**：当前根节点额外使用 WorldSpace Canvas 和 X=90° 旋转，是因为 Gameplay 进度条挂在卡牌视图下、需要贴合 StackCraft XZ 桌面；它替代模板的 `WorldCanvas` 父级，不改变单条进度条可见参数。`gameplay-static-preflight` 已按 `ProgressUI` / `ProgressFill` 对账背景、填充、字段引用和 `Square.png` GUID，但不能替代最终画面诊断。 |
| 烟雾粒子 | `PuffParticle.prefab` + `Puff.mat` + `Puff.png` + `Puff.wav`；`PuffParticle.Awake` 播放 `AudioId.Puff`，并在 `main.duration + 0.1f` 后销毁；`CardInstance.PlayPuffParticle`、制作产出、交易和击杀等位置实例化。 | `Assets/Art/Prefabs/卡牌烟雾粒子.prefab` 由参考 `PuffParticle.prefab` 克隆粒子参数后替换为 `TabletopCardSmokeEffectView`；材质 / 贴图 / 音效迁入 `Assets/Art` / `Assets/Audio`，贴图和音效字节与 StackCraft 源文件一致；`TabletopPresentationCueKind.CardSmoke` 由牌桌视图先播放 `m_cardSmokeAudio`，再在对应卡牌坐标实例化粒子，播放结束后隐藏并由 ResourceSystem 释放。 | **已登记并受对象级静态守卫覆盖，结构差异已裁决**：StackCraft 的粒子 Prefab 依赖 `PuffParticle.Awake` 自动播放音效并 Destroy；Gameplay 采用正式表现提示、视图设置和资源释放链承接同等“音效 + 粒子 + 生命周期”效果，生命周期由粒子系统 `duration + startLifetime` 派生，不保留 StackCraft 旧单例音频入口或手写 `+0.1f` 余量。`gameplay-static-preflight` 已按 `PuffParticle` 对账 Transform、ParticleSystem、ParticleSystemRenderer、排序、材质、`m_particleSystem` / `m_renderer` 字段引用、贴图 / 导入参数 / 音效哈希、音频映射和生命周期源码；最终画面仍留到整体验收截图。 |
| 受击反馈 | `CardInstance.TakeDamage` 在扣血和刷新生命后执行表现：先 Kill 上一次 `_hurtTween`，材质 `_FlashAmount` 延迟 `0.05` 秒后用 `DOFloat(1, "_FlashAmount", 0.1)`，`SetLoops(2, Yoyo)`；同时卡牌根对象 `DOPunchRotation(new Vector3(0, 15, 0), 0.25, vibrato: 25)`；两段 Join 到同一 `Sequence` 并 `SetUpdate(true)`。 | `TabletopView` 在 GAS 伤害表现事件解析到目标卡牌后，只对“非 Miss 且实际伤害大于 0”的结果调用 `TabletopCardView.PlayHurtFeedback()`；`TabletopCardView` 先 Kill 上一次受击 Tween，闪白用 `DOTween.To(..., SetSurfaceFlash, 1f, m_hurtFlashTweenSeconds)` 写入 `_FlashAmount` 的 `MaterialPropertyBlock`，摇晃用 `DOPunchRotation(new Vector3(0f, m_hurtPunchRotationDegrees, 0f), m_hurtPunchDurationSeconds, m_hurtPunchVibrato)`；`卡牌视图.prefab` 已序列化 `0.05 / 0.1 / 2 / 15 / 0.25 / 25`。 | **已登记并受静态守卫覆盖，结构差异已裁决**：StackCraft 直接改 Renderer 材质实例，Gameplay 用材质属性块避免运行时材质实例副作用，玩家可见输入属性仍是 `_FlashAmount`；伤害结算真相归 EX-GAS，卡牌视图只承接表现。`gameplay-static-preflight` 已守卫 DOTween 链、序列化参数和旧手写近似算法残留；测试只能补充触发链路，不能替代参数闭包。 |
| 命中 UI | `HitUI.prefab` + `HitUI.Initialize`：根尺寸 `0.4 × 0.4`，实例化旋转 `Quaternion.Euler(90,0,0)`；Miss / Normal / Critical 使用 `Hit_Miss.png`、`Hit_Normal.png`、`Hit_Critical.png`，优势 / 劣势使用 `Effectiveness_Advantage.png`、`Effectiveness_Disadvantage.png`；克制图标锚在右上，位置 `{0.15,0}`、尺寸 `0.15 × 0.15`；Miss 伤害文本为空，命中显示伤害数字；伤害文字在模板里使用 `LiberationSans SDF`、Geometry 对齐、字号基准 `0.2`；`DOPunchScale(new Vector3(0.15,0.15), 1)`、`SetUpdate(true)` 后销毁。 | `TabletopHitResultView` 作为独立浮动 UI 承接模板命中结果；测试场景视图设置通过 `m_hitResultViewPrefab` 引用正式 `命中结果.prefab`；Prefab 初始隐藏，运行时设置本地位置和 `x=90°` 旋转；五张图标已复制到 `Assets/Art/Sprites` 并由 Prefab 字段引用自有 GUID；伤害文字使用项目中文 TMP 字体承载内容，TMP 对齐、字号、根尺寸、克制图标位置 / 尺寸、`m_punchScale = 0.15`、`m_punchDurationSeconds = 1` 均回写为 StackCraft 参数。 | **已登记并受对象级静态守卫覆盖，结构差异已裁决**：StackCraft 播放完成后 Destroy，Gameplay 为 ResourceSystem 实例复用链路，播放完成后隐藏并由 `TabletopView` 释放实例；卡牌本体只保留受击闪白 / 摇晃。`gameplay-static-preflight` 已按 `HitUI`、`DamageLabel`、`Effectiveness` 对账根图标、伤害文字效果参数、克制图标、字段引用、DOTween punch、五张图标哈希、中文字体引用和 Prefab GUID；测试只补充触发链路。 |
| 投射物 | `Projectile_Arrow.prefab` / `Projectile_Magic.prefab` 使用 `Projectile_Arrow.png` / `Projectile_Magic.png`，SpriteRenderer 尺寸 `1.28 × 1.28`；脚本默认 `duration = 0.6`，但两个 Prefab 实际序列化 `duration: 0.5`，对账以 Prefab 实值为准；运行时 `CombatProjectile.Fire` 先设置起点，再用 `(target - start).Flatten()`、`Quaternion.LookRotation(direction) * Quaternion.Euler(90,0,0)` 朝向目标，`DOMove(targetPos, duration)` + `Ease.Linear` + `SetUpdate(true)`，完成后 Destroy。 | `投射物.prefab` 接入 `箭矢投射物.png` / `魔法投射物.png` 自有副本，文件哈希与 StackCraft 源图一致但 GUID 独立；Prefab 回写 SpriteRenderer 尺寸 `1.28 × 1.28`，`TabletopProjectileView` 按 `Combat_Ranged` / `Combat_Magic` 选择图片，使用 `DOLocalMove(end, durationSeconds)` + `Ease.Linear` + `SetUpdate(true)`，完成后隐藏并由 `TabletopView` 释放实例；朝向公式保留水平面归零和 `Euler(90,0,0)`。 | **已登记并受对象级静态守卫覆盖，结构差异已裁决**：StackCraft 投射物挂在 `WorldCanvas` 下走世界坐标 `DOMove`，Gameplay 投射物挂在牌桌视图根下走本地坐标 `DOLocalMove`，两者都沿 StackCraft XZ 桌面完成同款 0.5 秒线性飞行；StackCraft 完成后 Destroy，Gameplay 完成后隐藏并释放 ResourceSystem 实例。`gameplay-static-preflight` 已对账箭矢 / 魔法源 Prefab 共享参数、当前 `投射物` SpriteRenderer、`m_renderer` 字段引用、两张图片哈希、GUID、尺寸和手写 Lerp 残留。 |
| 镜头聚焦 / 命中震动 | `CameraController` 使用透视相机控制根；`MoveTo` 用 `DOMove(..., 0.5)` 聚焦到目标，平移 / 缩放参数为 `panSpeed = 0.01`、`smoothTime = 0.15`、`panPadding = 0.5`、`zoomSpeed = 1`、`minDistance = 5`、`maxDistance = 20`；`CombatTask` 命中后调用 `CameraController.Shake()`，默认 `duration = 0.3`、`strength = 0.1`。 | `FoundationTest` 生成同形 `CameraController` 根 + 子 `Main Camera`；`TabletopCameraController` 使用 StackCraft 平移 / 缩放 / 聚焦公式；命中震动由现有 `GameCore.CameraShake` 监听 `AbilitySystemDamageResolvedPresentationEvent` 承接，测试场景回写 `m_amplitude = 0.1`、`m_duration = 0.3`，测试配置启用 `AbilitySystemDamageResolved` 震动来源。 | **已登记并受对象 / 组件级静态守卫覆盖，结构差异已裁决**：StackCraft 在旧 CombatTask 内直接拿 CameraController 调用 Shake，Gameplay 不把震动并回牌桌镜头控制器，而是由 GameCore 正式表现事件统一驱动；玩家可见参数和命中触发条件保持同等。`gameplay-static-preflight` 已按 `CameraController`、`Main Camera`、`TabletopCameraController` 和 `GameCore.CameraShake` 组件字段守卫透视相机层级、MoveTo 参数、平移 / 缩放公式、镜头震动参数和 GAS 命中事件来源；最终画面构图和震动观感仍随整体验收截图 / 录像确认。 |
| 收购点 / CardBuyer 表面 | `CardBuyer.prefab` 使用 `TradeZone.mat` 作为根卡面材质，根 Collider 为 `0.8 × 0 × 1.0000004`；运行时 `TradeManager` 对交易区统一设置 `zoneScale = {1.125,1,1.125}`，所以最终可见尺寸为 `0.9 × 1.1250004`；标题子物体文本为 `Sell`、位置 `{0,0,-0.35}`、尺寸 `{0.8,0.3}`、字号 `1.5`；`Icon` 子物体使用内置 Quad、位置 `{0,0.001,0.15}`、旋转 `x = 90`、缩放 `{0.7,0.7,1}` 和 `CurrencyIcon.mat`；`CardBuyer.Initialize` 会把货币卡图写入图标材质 `_MainTex`。 | 当前收购点由 `CardBuyerDefinition : CardDefinition` 声明货币内容 ID，并由 `地基日终收购点.asset` 的作者源尺寸字段按 `0.8 × 1.0000004 × 1.125` 覆盖最终可见尺寸；`TabletopCardView.ApplySize` 按 Prefab 未缩放本体尺寸缩放根对象，避免只放大碰撞盒而卡面仍是 0.8 × 1.0；`地基日终收购点.asset` 使用 `CardBuyerDefinition`、卡图 `CardArts_Placeholder`、表面材质 `交易区`；`交易区.mat` / `货币图标.mat` 是 StackCraft `TradeZone.mat` / `CurrencyIcon.mat` 的自有副本；`TabletopCardView.ApplyCardBuyerSurface` 把标题投影为 `Sell`、隐藏价格 / 营养文本、用货币卡图写入收购货币图标；`FoundationTestSceneMenu` 重建 `卡牌视图.prefab` 时生成 `收购货币图标` 子物体并写入序列化引用。 | **已登记并受静态守卫覆盖**：源码最终尺寸覆盖、根对象缩放入口、Prefab 字段、材质参数、YooAsset 收集项和测试作者源已由 `gameplay-static-preflight` 守卫；2026-08-20 Unity 编译 `0` 错误，正式 `卡牌视图.prefab` 可由 UnitySkills 回读。最终交易区画面仍留到整体验收截图。 |
| 商贩 / 卡包表面 | `PackVendor.prefab`、`PackInstance.prefab`、交易区高亮和烟雾；`PackVendor` 根 Collider 为 `0.8 × 0 × 1.0000004`，运行时 `TradeManager.zoneScale = {1.125,1,1.125}` 后最终可见尺寸为 `0.9 × 1.1250004`；`Board.TopMargin = 1.5`，`TradeManager` 把交易区根放到 `bounds.max.z - topMargin * 0.5`，并按 `spacing = 1.1` 横向排布收购点和商贩；三段文本分别为标题 `{0,0,0}` / `0.7×0.4` / 字号 `1.2`、进度 `{0,0,0.35}` / `0.8×0.3` / 字号 `1.1`、价格 `{0,0,-0.35}` / `0.8×0.3` / 字号 `1.1`，价格文本为 `Price: {剩余价格}`，进度文本为 `Discovered:\n{已发现}/{总数}` 或金色 `COMPLETED`；支付可分次，满价后在商贩下方生成卡包并重置已付金额；`TradeZone.TryTradeAndConsumeStack` 在 `CanTrade` 成功后先播放 `puffParticle`，再执行交易；`PackInstance.prefab` 使用 `Pack.mat`、独立 Collider `0.9 × 0 × 1.3000002`、独立标题落点和 `PackArts` 卡包图。 | 当前商贩链由 `PackVendorDefinition` / `PackVendorCard` + 购买行动 + `TabletopCardView.ApplyPackVendorSurface` 承接；商贩测试作者源按 `0.8 × 1.0000004 × 1.125` 覆盖最终可见尺寸，`TabletopCardView.ApplySize` 将该尺寸转换为根对象缩放，使交易区 Mesh、三段文本、货币图标 / 商贩卡面和 Collider 同步放大；统一测试场景按 `12×8` 的 StackCraft Board 运行时规则边界推导 `Board.TopMargin = 1.5` 的页眉中心，并按 `spacing = 1.1` 创建锁定收购点与商贩，锁定工位允许位于页眉禁放区，普通卡仍被页眉禁放区排除；付款卡按 `spawnOffset = {0,-1.4}` 放在对应交易区下方；卡包实体由 `CardPackDefinition.GetViewSize()`、`卡牌表面_卡包.mat`、`TabletopCardView.ApplyCardPackInstanceSurface()` 和 YooAsset 地址 `PackArts_Starter` / `PackArts_Beginning` 承接。目标 PlayMode 的 `Price: 2 → 1 → 2` 是 Starter 商贩双付款测试夹具，用来验证 StackCraft 分次付款、交易高亮、烟雾和生成偏移语义，不代表 Starter 卡包原始 `buyPrice`；Beginning 商贩的价格和解锁任务数继续从 StackCraft `01_Pack_Beginning.asset` 派生对账。购买生成卡包后继续验证标题 / 材质 / 卡图 / 独立尺寸 / Collider，并验证只有金币拖到商贩时按 StackCraft `CanTrade` 语义高亮，普通非付款卡拖到商贩时不高亮；购买行动结算会为每次有效付款生成 `CardSmoke` 表现提示，牌桌视图在商贩牌桌坐标播放 `卡牌烟雾粒子`。 | **源码 / 来源对象参数已登记并受静态守卫覆盖，结构差异已裁决**：交易区最终尺寸、根对象缩放入口、Board 运行时规则边界、页眉禁放区、锁定交易工位、横向间距、`PackVendor.prefab` 三段文本的 Transform / 尺寸 / 字号来源、`PackInstance.prefab` 标题和碰撞盒来源、分次购买链、购买生成位置、交易区高亮、卡包独立表面和两次成功购买烟雾已由 StackCraft 源码 / Prefab 对账与静态守卫覆盖；Starter 双付款价格只作为测试夹具参数，目标 PlayMode 只作为链路补充证据，最终画面仍留到整体验收截图。 |
| 商贩解锁序列 | `TradeManager.PlayActivationSequence` 在完成任务数解锁商贩时：显示 `Pack Unlocked` 与 `You can buy {PackName} card pack here.`，高亮商贩，调用 `CameraController.MoveTo`，等待 `2f` 秒后清除高亮；同时用模板 `InputManager` / `TimeManager` 暂停玩家输入与时间。 | 当前 `ScenarioRun` 在任务事实刷新后检测完成任务数跨过 `PackVendorDefinition.MinimumCompletedQuests`，发布 `ScenarioSequencePresentationRequestEvent`；`ScenarioDirector` 用队列串行播放请求，期间通过 `GameCore.InputSystem` 的 Gameplay 输入锁和 `GameCore.GameStateSystem` 的外部暂停锁接管 2 秒，再发布 `ScenarioSequenceMessageEvent` 给 `TabletopCardInfoPanel`，让详情面板按 StackCraft `InfoPanel` 的优先级语义用流程提示覆盖悬浮 / 选中卡牌详情，到期后恢复当前可读卡牌；同时向当前牌桌发布 `CameraFocus` 与 `CardHighlight`；牌桌拖拽输入和镜头只读取正式输入锁状态。 | **源码映射已登记并受静态守卫覆盖，结构差异已裁决**：模板的旧输入 / 时间单例由 GameCore 正式输入锁和暂停锁替代，2 秒提示、镜头聚焦和商贩高亮由 `gameplay-static-preflight` 守卫；底部回合 HUD 不再保存第二份流程提示显示状态。2026-08-20 Unity 编译和 Console 错误均为 `0` 的历史证据，最终画面仍待整体验收截图 / 录像。 |

## 下一步顺序

1. **卡牌表面优先**：先对齐卡牌类别材质 / 卡图比例 / 标题、价格、营养、生命文字的可见承载，避免还没静止画面就讨论动画。
2. **必要反馈第二**：再对齐候选高亮、行动进度、命中 UI、烟雾粒子、投射物素材和镜头聚焦。
3. **动作生命周期第三**：最后对齐拖拽抬升、移动补间、受击闪烁 / 摇晃、投射物飞行、命中结果 punch 和销毁时序。
4. **验证只按覆盖范围声明**：静态源码 / Prefab 对照证明结构差异；Unity 编译证明资源可用；PlayMode 证明链路可跑；截图只能证明当前画面，不能证明未截图动画段。

## 当前阻塞

当前没有阻塞本轮 C7 目标对照的事项。剩余边界是删除模板前的独立门槛：依赖扫描、用户授权、删除 `Assets/StackCraft` 后的静态预检、Unity 编译和必要 PlayMode 复验；连续手感如果要达到正式视觉验收，需要另行录像或试玩确认。

## 静态守卫

`node .spec/tools/gameplay-static-preflight.mjs` 当前额外检查：

- `Assets/Art/Sprites/StackCraft` 必须由 YooAsset 以 `AddressByFolderAndFileName` 收集，确保 `Cards/Character.png` 等覆盖图来源可加载。
- YooAsset 收集器必须按 Collector 块字段级对账，不能只证明路径和规则在全文件分别出现；`Assets/Art/Sprites/StackCraft`、正式表现 Prefab、正式 UI Prefab、卡牌表面材质、卡包材质、交易区材质和测试占位卡图都必须在自身收集块内带有期望地址规则、打包规则、过滤规则和 `test` 标签。
- `Assets/StackCraft/Textures` 复制到 `Assets/Art/Sprites/StackCraft` 的全部贴图必须同时满足图片 hash 一致和 Unity 导入视觉参数一致；导入参数包括 mipmap、wrap、非 2 次幂缩放、透明通道和压缩设置。自有 `.meta` 必须保留项目新 GUID、`textureType: 8`、`spriteMode: 1` 和 Sprite 引用能力，不能整份复用 StackCraft `.meta`。
- `Assets/Art/Sprites/CardArts` 中已中文化并被测试内容直接使用的 StackCraft 卡图副本，也必须同时满足图片 hash 一致和 Unity 导入视觉参数一致；当前覆盖村民、哥布林、鸡、鸡蛋、木头、石头、金币、宝箱、Starter / Beginning 卡包等 19 张自有卡图。它们保留项目自有 GUID、`textureType: 8`、`spriteMode: 1` 和 Sprite 引用能力，不能退回来源的非 Sprite 导入类型。
- `Assets/Art/Sprites/卡牌占位图.png` 是测试场景直接消费的独立自有卡图，不属于 `Assets/Art/Sprites/StackCraft/**` 目录扫描；必须按 StackCraft `Sprites/Square.png` 同时对账图片 hash、Unity 导入视觉参数、YooAsset 收集路径和 `FoundationTestSceneMenu` 消费入口。
- `Assets/StackCraft/Textures` 中被迁到 `Assets/Art/Textures` 的材质贴图副本也必须同时满足图片 hash 一致和 Unity 导入视觉参数一致；当前覆盖 `草地背景.png`、`水面背景.png` 和 `卡牌烟雾.png`，并额外要求 `textureType` / `spriteMode` 与 StackCraft 来源一致，避免材质贴图被错误导入为 Sprite。
- StackCraft fbx 模型副本必须同时对账模型文件 hash 和 Unity 模型导入参数，包括网格尺寸、法线、切线、动画导入和材质导入语义；允许 `Board.fbx` 的内置材质 remap 指向项目自有牌桌材质。
- StackCraft 12 张卡牌分族贴图和 12 个自有类别材质必须存在并保留 `.meta`；12 个自有类别材质的 `_BaseTex`、`_MainTex`、`_OverlayTex` 必须逐槽映射 StackCraft 来源材质引用的贴图，并引用 `Assets/Art/Sprites/StackCraft` 下的自有复制素材 GUID。
- 测试作者源里的 `m_cardSurface` 地址必须落在 `卡牌表面_*` 材质地址白名单内，不得再使用 `Cards_*` 图片地址。
- 卡牌视图 Prefab 必须使用 `MeshFilter` + `MeshRenderer` + 自有 `卡牌.fbx` + 自有 `卡牌表面_角色.mat`；`TabletopCardView` 的 `m_surfaceRenderer`、`m_surfaceMeshFilter`、`m_defaultSurfaceMesh`、`m_packSurfaceMesh` 和 `m_highlightMeshFilter` 必须字段级指向正确组件 / 自有网格；`TabletopCardView.ApplySize` 必须从未缩放 Collider / Mesh 本体尺寸推导根对象缩放，确保交易区和卡包的 Mesh、文本、图标与命中范围同步放大，不得只改碰撞盒；卡面文字必须使用项目中文 TMP 字体承载内容，并按 StackCraft 来源对账文字位置、尺寸、字号、对齐、边距和样式参数；候选高亮必须引用自有 `卡牌轮廓.shadergraph` 子资源；自有 `卡牌表面.shadergraph` / `卡牌轮廓.shadergraph` 必须与 StackCraft 原 shadergraph 文件哈希一致，并保持 ShaderGraph `ScriptedImporter` 脚本一致但使用项目自有 GUID；不得保留独立 SpriteRenderer 插画层、旧 `m_artworkRenderer`、旧 `m_artworkPadding`、旧“表面详情”混合文本节点、整块绿色候选遮罩或四条 Sprite 线框高亮。
- 牌桌二维坐标必须经 `TabletopCoordinateSpace` 映射到 StackCraft XZ 桌面；静态卡牌、拖拽释放点、战斗阵型断言和战斗区域视图不得回退为 `new Vector3(x, y, 0)`；战斗区域 Prefab 必须保持 `x = 90°` 旋转贴到桌面，颜色来自当前生成器的本地适配，不作为 StackCraft 来源参数。
- `FoundationTest`、`FoundationMapTest`、`FoundationSecondMapTest` 当前场景和重建生成器必须包含自有 `桌面背景` 和 `牌桌底板` 纯表现节点，并对象级检查各自 Transform、Mesh / SkinnedMeshRenderer、材质和渲染 AABB：Main / Map 使用 StackCraft Grass + Board01，SecondMap 使用 StackCraft Water + Board02；四个测试地区作者源必须使用 StackCraft `Board.cs` 运行时 `BakeMesh` 对应的 `12 × 8` 规则边界和顶部 `1.5` 页眉禁放区；固定交易工位必须锁定创建并允许位于页眉内；不得引用 StackCraft 原 GUID 或旧 `Board` 脚本。
- `TradeManager.cs`、`CardBuyer.prefab`、`PackVendor.prefab`、`PackInstance.prefab` 和 `HitUI.prefab` 的交易区 / 卡包 / 命中表面参数必须从 StackCraft 来源源码与来源对象提取后对账当前运行时常量和目标 Prefab 字段：交易区缩放、横向间距、生成偏移、收购点 / 商贩根碰撞盒尺寸、商贩 Title / Tracker / Price 的位置、尺寸、字号、TMP 文本样式 hash，以及卡包标题位置、尺寸、边距、颜色、字号和根碰撞盒尺寸；不得只用手写 token 证明收购点、商贩、卡包或命中结果表面复刻。
- HUD、命中结果、行动进度、烟雾粒子和投射物 Prefab 的静态对账必须命中具体对象、组件和字段引用：`CardStatsUI` 三组图标 / 数字、`HitUI` 根图标 / `DamageLabel` / `Effectiveness`、`ProgressUI` 背景 / `ProgressFill`、`PuffParticle` 的 ParticleSystem / Renderer、`Projectile_Arrow` / `Projectile_Magic` 与当前 `投射物` SpriteRenderer；对应图标 / 方块 / 投射物图片还必须对账导入视觉参数；全文件 token 只能作为 smoke。
- 材质与引用闭包必须按字段验证：卡牌 / 卡包 / 交易区 / 装备 / 烟雾 / 桌面材质的 `m_Shader` 与贴图槽，模型 `.meta` 的外部材质映射，视图设置的 `SoftAssetReference`，以及音效资产的 `m_audioClips` 列表都不能只靠 GUID 全文件搜索证明。
- StackCraft 音频副本必须同时对账 wav 字节和 Unity 音频导入参数，包括加载类型、采样率、压缩、预加载、后台加载和 3D 音频设置；不得只用音频文件 hash 证明播放效果已锁定。
- 命中结果 Prefab 的伤害文字必须使用项目中文 TMP 字体承载内容，并保持 StackCraft 的 Geometry 对齐、`0.2` 字号基准、`0.15` punch 缩放和 `1` 秒 punch 时长。
- 牌桌测试视图设置资产必须回写 StackCraft 牌堆视觉与输入参数：每层抬升 `0.002`、拖拽跟随锐度 `100`、点击判定距离 `0.02`、拖拽抬升高度 `0.1`、普通移动秒数 `0.1`；源码必须按上一张卡当前表现位置加 StackCraft 步进生成尾随卡拖拽姿态，按牌桌世界距离判断释放解释，并在按下后立即预览实际被拖走牌段。
- 行动进度 Prefab 必须使用 StackCraft 同类 WorldSpace Canvas + UGUI Image / `fillAmount` 语义，引用自有 `Square.png` 副本，保持灰底、黄色填充和 `{0,0,0.55}` 显示偏移；不得回退到 SpriteRenderer 缩放或占位卡图。
- 投射物 Prefab 必须引用箭矢 / 魔法图片，`m_renderer` 必须指向本对象 SpriteRenderer，不能退回旧占位缩放。
